import 'package:flutter/material.dart';

class NotifiCations extends StatelessWidget {
  const NotifiCations({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ɴᴏᴛɪꜰɪᴄᴀᴛɪᴏɴ'),
      ),
    );
  }
}
